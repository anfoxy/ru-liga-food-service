management:
 endpoints:
  web:
   exposure:
    include: "*" #<2>
 endpoint:
  health:
   show-details: ALWAYS

spring:
 security:
  oauth2:
   resourceserver:
    jwt:
     issuer-uri: http://localhost:9000